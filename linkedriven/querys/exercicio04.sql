SELECT u.id as id, u.name as name, r.name as role, c.name as company, e."startDate" FROM users u JOIN experiences e ON e."userId" = u.id JOIN roles r ON r.id = e."roleId" JOIN companies c ON c.id = e."companyId" WHERE u.id = 50 AND e."endDate" IS NULL;